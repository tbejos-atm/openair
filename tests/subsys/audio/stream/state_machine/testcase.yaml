# Copyright (c) 2026 Atmosic
#
## SPDX-License-Identifier: LicenseRef-Atmosic
#
common:
  # boards/common.overlay dereferences &i2s, so the overlay only compiles on a
  # SoC declaring that node.
  depends_on:
    - i2s
tests:
  tests.subsys.audio.stream.state_machine.atm: &base_config
    sysbuild: true
    tags:
      - audio
  tests.subsys.audio.stream.state_machine.atm.gcov:
    <<: *base_config
    tags:
      - gcov
    depends_on:
      - gcov_capable
    timeout: 180
    extra_configs:
      - CONFIG_UU_TEST_OPENAIR="audio_stream.c"
